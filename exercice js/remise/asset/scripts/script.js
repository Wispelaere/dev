let pu = prompt("Veuillez saisir le prix unitaire");
let qtecom = prompt("veuillez saisir la quantité commandée");
let totnr = (pu*qtecom);
let port= 0;
let remise = 0;
let pap =0;
let total_r_p;

if (tot > 100 && tot < 200){
    
}